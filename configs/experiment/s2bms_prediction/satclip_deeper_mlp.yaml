# @package _global_

# to execute this experiment run:
# python train.py experiment=example

# Satclip and MLP prediction head, with 3 random seeds

defaults:
  - override /model: s2bms_prediction
  - override /data: s2bms_prediction
  - override /metrics: s2bms_predictive

tags:
  - prediction
  - satclip
  - ${str:${seed}}

experiment_name: satclip_deeper_mlp

model:
  geo_encoder:
    _target_: src.models.components.geo_encoders.satclip.SatClipCoordinateEncoder
    geo_data_name: coords
    hf_cache_dir: ${paths.huggingface_cache}
  trainable_modules: [prediction_head]
  prediction_head:
    _target_: src.models.components.pred_heads.mlp_pred_head.MLPPredictionHead
    nn_layers: 3

data:
  dataset:
    modalities:
      coords:
    use_target_data: true
    use_aux_data: false
    dtype: float64
  caption_builder:
  pin_memory: true
  num_workers: 14
  batch_size: 128
  persistent_workers: true
  saved_split_file_name: "s2bms_union_val_test.pth"

logger:
  wandb:
    tags: ${tags}
    group: "predictive"
    project: "s2bms_prediction"
  aim:
    experiment: "predictive"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 12345, 404, 654
