# @package _global_

# to execute this experiment run:
# python train.py experiment=example

# AEF with resnet (18 or 50) encoder and MLP prediction head, for tile sizes 128 and 256, each with 3 random seeds

defaults:
  - override /model: s2bms_prediction
  - override /data: s2bms_prediction
  - override /metrics: s2bms_predictive

tags:
  - prediction
  - aef
  - aef_${str:${data.dataset.modalities.aef.size}}
  - ${str:${seed}}

experiment_name: aef_${str:${data.dataset.modalities.aef.size}}_rn${str:${model.geo_encoder.resnet_version}}_deeper_mlp

model:
  geo_encoder:
    _target_: src.models.components.geo_encoders.cnn_encoder.CNNEncoder
    geo_data_name: aef
    input_n_bands: 64
    pretrained_cnn:
  prediction_head:
    _target_: src.models.components.pred_heads.mlp_pred_head.MLPPredictionHead
    nn_layers: 3
  trainable_modules: [geo_encoder, prediction_head]

data:
  dataset:
    modalities:
      aef:
        format: npy
    use_target_data: true
    use_aux_data: false
    dtype: float32
  caption_builder:
  pin_memory: true
  num_workers: 14
  batch_size: 128
  persistent_workers: true
  prefetch_factor: 4
  saved_split_file_name: "s2bms_union_val_test.pth"

logger:
  wandb:
    tags: ${tags}
    group: "predictive"
    project: "s2bms_prediction"
  aim:
    experiment: "predictive"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 12345, 404, 654
      ++data.dataset.modalities.aef.size: 128, 256
      ++model.geo_encoder.resnet_version: 18, 34, 50
