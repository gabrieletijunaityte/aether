# @package _global_

# to execute this experiment run:
# python train.py experiment=example

# tabular only

defaults:
  - override /model: s2bms_prediction
  - override /data: satbird-usa
  - override /metrics: satbird_predictive

tags:
  - prediction
  - tabular
  - ${str:${seed}}

experiment_name: tabular_baseline_linear

model:
  normalize_features: false
  standardize_targets: false
  geo_encoder:
    _target_: src.models.components.geo_encoders.identity_encoder.IdentityEncoder
    geo_data_name: satbird-USA-target
  trainable_modules: [geo_encoder, prediction_head]
  prediction_head:
    _target_: src.models.components.pred_heads.linear_pred_head.LinearPredictionHead

data:
  dataset:
    modalities:
      coords:
    use_features:
      pattern: "^aux_(?!.*top).*"
      stats_file: ${paths.data_dir}/satbird-USA-summer/aux_stats/labelled-summer.json
    use_target_data: true
    use_aux_data: false
  caption_builder:
  pin_memory: true
  num_workers: 11
  prefetch_factor: 6
  batch_size: 512
  persistent_workers: true
  saved_split_file_name: "satbird-USA-summer_aef_union_val_test.pth"

logger:
  wandb:
    tags: ${tags}
    group: "predictive"
    project: "satbird-usa-summer_prediction"
  aim:
    experiment: "predictive"

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      seed: 12345, 404, 654
